<odoo>
    <!-- Wizard Form View for Room Availability -->
    <record id="view_room_availability_wizard_form" model="ir.ui.view">
        <field name="name">room.availability.wizard.form</field>
        <field name="model">room.availability.wizard</field>
        <field name="arch" type="xml">
            <form string="Room Availability">
                <group>
                    <field name="start_date"/>
                    <field name="end_date"/>
                </group>
                <footer>
                    <button string="View Report" type="object" name="action_view_report" class="btn-primary"/>
                    <button string="Print Report" type="object" name="action_print_report" class="btn-secondary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action for Room Availability Report Wizard -->
    <record id="action_room_availability_wizard" model="ir.actions.act_window">
        <field name="name">Room Availability Wizard</field>
        <field name="res_model">room.availability.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_room_availability_wizard_form"/>
        <field name="target">new</field>
    </record>
</odoo>